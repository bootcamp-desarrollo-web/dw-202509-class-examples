let myArr = ['Dani', 'Elinor', 'María', 'Jorge'];

console.log(myArr.length)

// Imprimir el último elemento
console.log(myArr[myArr.length - 1]);

// Iterar sobre todo el array e imprimir los nombres unos por uno
for (let i = 0; i < myArr.length ; i++) {
  console.log(myArr[i]);
}  
