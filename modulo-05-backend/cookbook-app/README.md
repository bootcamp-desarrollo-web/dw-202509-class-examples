## Pasos necesarios para la instalación de la aplicación:
1. Ejecutar npm install
2. crear un fichero `.env` (referencia: `.env.example`)
3. Crear una BBDD con el nombre 'cookbook' con dos colecciones: `categories` y `recipes`