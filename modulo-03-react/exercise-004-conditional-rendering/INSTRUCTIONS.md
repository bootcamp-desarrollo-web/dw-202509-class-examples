# Ejercicio renderizado condicional

Vamos a crear una versión simple del juego "7 boom". La idea es que haya un botón que permita incrementar un contador, y cada vez que el contador sea un multiplo de 7 se muestre la palabra "Boom".

El contador y el botón tiene que estar dentro de `App.jsx`.  
El valor del contador se tiene que pasar como propiedad al componente `Boom.jsx`.  

### Extra (1):  
Muestra la palabra "Boom" si aparece también la cifra 7 dentro del contador.

### Extra (2):  
Añade algún estílo y/o emoji para hacer la app más atractiva 😊